<%- include ('partials/header') %> 
<%- include ('partials/navbar') %> 

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>Event Type<small>Control panel</small></h1>
        <ol class="breadcrumb">
            <li><a href="/cloud/dashboard"><i class="fa fa-dashboard"></i> Home</a></li>
            <li class="active">Event Type</li>
        </ol>
    </section>
    <section class="content">
        <div class="row">
            <% for( let i = 0; i < project.length; i++ ) { %>
            <div class="col-md-12">
                <div class="box">
                    <div class="box-header with-border">
                        <h3 class="box-title"><%= project[i].project_name %> </h3>
                        <div class="box-tools pull-right">
                            <button type="button" class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-minus"></i></button>
                            <button type="button" class="btn btn-box-tool" data-widget="remove"><i class="fa fa-times"></i></button>
                        </div>
                    </div>
                    <div class="box-body">
                        <% for( let j = 0; j < eventType.length; j++ ) { %>
                            <div class="box">
                                <div class="box-header with-border">
                                    <h3 class="box-title"><%= eventType[j].event_type_name %> </h3>
                                    <div class="box-tools pull-right">
                                        <button type="button" class="btn btn-box-tool" data-widget="collapse"><i class="fa fa-minus"></i></button>
                                        <button type="button" class="btn btn-box-tool" data-widget="remove"><i class="fa fa-times"></i></button>
                                    </div>
                                </div>
                                <div class="box-body">
                                    <pre><code>Dictionary&lt;string, string&gt; data = new Dictionary&lt;string, string&gt;();
data["object_name"] = "<%= eventType[j].event_type_name %>";
data["ProjectId"] = "<%= project[i].id %>";
data["EventTypeId"] = "<%= eventType[j].id %>";
socket.Emit("Action", new JSONObject(data));</code></pre>
                                </div>
                            </div>
                        <% } %>
                    </div><!-- /.box-body -->
                </div><!-- /.box -->
            </div>
            <!-- /.col-md-6 -->
            <!-- /.col (LEFT) -->
            <% } %>
        </div>
      <!-- /.row -->
    </section>
</div>

<%- include ('partials/footer') %> 